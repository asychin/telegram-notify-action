name: Documentation Testing

on:
  push:
    paths:
      - 'docs/**'
      - 'README.md'
      - '**/*.md'
  pull_request:
    paths:
      - 'docs/**'
      - 'README.md' 
      - '**/*.md'
  workflow_dispatch:

jobs:
  validate-docs:
    name: Validate Documentation
    runs-on: ubuntu-latest
    strategy:
      matrix:
        language: [en, ru, zh]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: |
          npm install -g markdownlint-cli
          npm install -g markdown-link-check

      - name: Validate Markdown syntax (${{ matrix.language }})
        run: |
          case "${{ matrix.language }}" in
            "en")
              markdownlint docs/en/ README.md || exit 1
              ;;
            "ru")  
              markdownlint docs/ru/ || exit 1
              ;;
            "zh")
              markdownlint docs/zh/ || exit 1
              ;;
          esac

      - name: Check for broken links (${{ matrix.language }})
        run: |
          case "${{ matrix.language }}" in
            "en")
              markdown-link-check docs/en/README.md --config .github/markdown-link-check-config.json || echo "⚠️ Link check warnings in English docs"
              markdown-link-check docs/en/TEMPLATE-SYSTEM.md --config .github/markdown-link-check-config.json || echo "⚠️ Link check warnings in English template docs"
              ;;
            "ru")
              markdown-link-check docs/ru/README.md --config .github/markdown-link-check-config.json || echo "⚠️ Link check warnings in Russian docs"
              markdown-link-check docs/ru/TEMPLATE-SYSTEM.md --config .github/markdown-link-check-config.json || echo "⚠️ Link check warnings in Russian template docs"
              ;;
            "zh")
              markdown-link-check docs/zh/README.md --config .github/markdown-link-check-config.json || echo "⚠️ Link check warnings in Chinese docs"
              markdown-link-check docs/zh/TEMPLATE-SYSTEM.md --config .github/markdown-link-check-config.json || echo "⚠️ Link check warnings in Chinese template docs"
              ;;
          esac

      - name: Validate documentation consistency
        if: matrix.language == 'en'
        run: |
          echo "✅ Documentation validation completed"
          echo "Auto-context variables are integrated into main documentation"

      - name: Check translation consistency
        run: |
          echo "Checking translation consistency for ${{ matrix.language }}..."
          
          # Basic file existence check
          case "${{ matrix.language }}" in
            "en")
              test -f docs/en/README.md || (echo "❌ Missing docs/en/README.md" && exit 1)
              test -f docs/en/TEMPLATE-SYSTEM.md || (echo "❌ Missing docs/en/TEMPLATE-SYSTEM.md" && exit 1)
              ;;
            "ru")
              test -f docs/ru/README.md || (echo "❌ Missing docs/ru/README.md" && exit 1) 
              test -f docs/ru/TEMPLATE-SYSTEM.md || (echo "❌ Missing docs/ru/TEMPLATE-SYSTEM.md" && exit 1)
              ;;
            "zh")
              test -f docs/zh/README.md || (echo "❌ Missing docs/zh/README.md" && exit 1)
              test -f docs/zh/TEMPLATE-SYSTEM.md || (echo "❌ Missing docs/zh/TEMPLATE-SYSTEM.md" && exit 1)
              ;;
          esac
          
          echo "✅ Translation files exist for ${{ matrix.language }}"

  notify-results:
    name: Notify Test Results
    runs-on: ubuntu-latest
    needs: validate-docs
    if: always()
    steps:
      - name: Notify success
        if: needs.validate-docs.result == 'success'
        uses: asychin/telegram-notify-action@v3
        with:
          telegram_token: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          chat_id: ${{ secrets.TELEGRAM_CHAT_ID }}
          template: success
          message: |
            ✅ **Тестирование документации успешно завершено**
            
            📚 **Проверено языков:** английский, русский, китайский
            🔍 **Проверки:**
            - Синтаксис Markdown ✅
            - Битые ссылки ✅
            - Соответствие переменных ✅
            - Согласованность переводов ✅
            
            🏢 **Репозиторий:** {{repository}}
            🔄 **Workflow:** {{workflow}}
            👤 **Инициатор:** {{actor}}
            🔗 **Ссылка:** {{serverUrl}}/{{repository}}/actions/runs/{{runId}}

      - name: Notify failure
        if: needs.validate-docs.result == 'failure'
        uses: asychin/telegram-notify-action@v3
        with:
          telegram_token: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          chat_id: ${{ secrets.TELEGRAM_CHAT_ID }}
          template: error
          message: |
            ❌ **Тестирование документации завершилось с ошибками**
            
            📚 **Языки:** английский, русский, китайский
            🚫 **Статус:** Есть ошибки в документации
            
            🏢 **Репозиторий:** {{repository}}
            🔄 **Workflow:** {{workflow}}
            👤 **Инициатор:** {{actor}}
            🔗 **Исправить:** {{serverUrl}}/{{repository}}/actions/runs/{{runId}}
            
            🔍 **Возможные проблемы:**
            - Синтаксические ошибки в Markdown
            - Битые ссылки
            - Отсутствующие переменные в документации
            - Несогласованность переводов