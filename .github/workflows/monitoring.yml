name: Repository Monitoring

on:
  issues:
    types: [opened, closed, reopened, labeled]
  issue_comment:
    types: [created]
  pull_request:
    types: [opened, closed, ready_for_review]
  pull_request_review:
    types: [submitted]
  discussion:
    types: [created, answered]
  schedule:
    # Daily health check at 9:00 UTC
    - cron: "0 9 * * *"
  workflow_dispatch:
    inputs:
      check_type:
        description: "Type of check to perform"
        required: false
        default: "health"
        type: choice
        options:
          - health
          - stats
          - security

jobs:
  issue-notifications:
    name: Issue Notifications
    runs-on: ubuntu-latest
    if: github.event_name == 'issues' || github.event_name == 'issue_comment'
    steps:
      - uses: actions/checkout@v4

      - name: Determine notification type
        id: notification-type
        run: |
          case "${{ github.event.action }}" in
            "opened")
              echo "template=warning" >> $GITHUB_OUTPUT
              ;;
            "closed")
              echo "template=success" >> $GITHUB_OUTPUT
              ;;
            "reopened")
              echo "template=warning" >> $GITHUB_OUTPUT
              ;;
            "labeled")
              echo "template=info" >> $GITHUB_OUTPUT
              ;;
            "created")
              echo "template=info" >> $GITHUB_OUTPUT
              ;;
          esac

      - name: Send issue notification
        if: github.event_name == 'issues'
        uses: ./
        with:
          telegram_token: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          chat_id: ${{ secrets.TELEGRAM_CHAT_ID }}
          template: ${{ steps.notification-type.outputs.template }}
          message: |
            🐛 **Issue {{action}}**
            
            **Заголовок:** {{issueTitle}}
            **Номер:** #{{issueNumber}}
            **Автор:** {{issueAuthor}}
            **Состояние:** {{issueState}}
            **Метки:** {{labels}}
            
            🏢 **Репозиторий:** {{repository}}
            👤 **Владелец:** {{repositoryOwner}}
            🔗 **Ссылка:** {{issueUrl}}
            
            📊 **Контекст GitHub:**
            - Workflow: {{workflow}}
            - Запуск: #{{runNumber}}
            - Актор: {{actor}}
            - Событие: {{eventName}}
            - Runner: {{runnerName}} на {{runnerOs}}
            
            📝 **Описание:**
            {{issueBody}}
          inline_keyboard: |
            [
              {"text": "👀 Посмотреть", "url": "{{issueUrl}}"},
              {"text": "💬 Комментировать", "url": "{{issueUrl}}#issuecomment-new"},
              {"text": "✏️ Редактировать", "url": "{{issueUrl}}"}
            ]

      - name: Send comment notification
        if: github.event_name == 'issue_comment'
        uses: ./
        with:
          telegram_token: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          chat_id: ${{ secrets.TELEGRAM_CHAT_ID }}
          template: info
          message: |
            💬 **Новый комментарий к Issue**
            
            **Issue:** #{{issueNumber}} - {{issueTitle}}
            **Автор комментария:** {{commentAuthor}}
            **Время:** {{commentCreatedAt}}
            
            🏢 **Репозиторий:** {{repository}}
            👤 **Владелец:** {{repositoryOwner}}
            
            📊 **Контекст:**
            - Workflow: {{workflow}}
            - Runner: {{runnerName}} ({{runnerOs}})
            - Актор: {{actor}}
            
            💭 **Комментарий:**
            {{commentBody}}
          inline_keyboard: |
            [
              {"text": "👀 Посмотреть", "url": "{{commentUrl}}"},
              {"text": "📝 Ответить", "url": "{{issueUrl}}#issuecomment-new"},
              {"text": "🔗 К Issue", "url": "{{issueUrl}}"}
            ]

  pr-notifications:
    name: Pull Request Notifications
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request' || github.event_name == 'pull_request_review'
    steps:
      - uses: actions/checkout@v4

      - name: Determine PR notification type
        id: pr-notification-type
        run: |
          case "${{ github.event.action }}" in
            "opened")
              echo "template=info" >> $GITHUB_OUTPUT
              ;;
            "closed")
              if [ "${{ github.event.pull_request.merged }}" = "true" ]; then
                echo "template=success" >> $GITHUB_OUTPUT
              else
                echo "template=warning" >> $GITHUB_OUTPUT
              fi
              ;;
            "ready_for_review")
              echo "template=info" >> $GITHUB_OUTPUT
              ;;
            "submitted")
              echo "template=info" >> $GITHUB_OUTPUT
              ;;
          esac

      - name: Send PR notification
        if: github.event_name == 'pull_request'
        uses: ./
        with:
          telegram_token: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          chat_id: ${{ secrets.TELEGRAM_CHAT_ID }}
          template: ${{ steps.pr-notification-type.outputs.template }}
          message: |
            🔄 **Pull Request {{prAction}}**
            
            **Заголовок:** {{prTitle}}
            **Номер:** #{{prNumber}}
            **Автор:** {{prAuthor}}
            **Состояние:** {{prState}}
            
            🔀 **Ветки:**
            - Из: {{prHeadRef}} ({{prHeadSha}})
            - В: {{prBaseRef}}
            
            📊 **Статистика:**
            - Коммиты: {{prCommits}}
            - Изменения: +{{prAdditions}} -{{prDeletions}}
            - Файлов: {{prChangedFiles}}
            
            🏢 **Репозиторий:** {{repository}}
            👤 **Владелец:** {{repositoryOwner}}
            🔗 **Ссылка:** {{prUrl}}
            
            📋 **Контекст GitHub:**
            - Workflow: {{workflow}}
            - Запуск: #{{runNumber}}
            - Runner: {{runnerName}} на {{runnerOs}}
            - Актор: {{actor}}
            
            📝 **Описание:**
            {{prBody}}
          inline_keyboard: |
            [
              {"text": "👀 Посмотреть", "url": "{{prUrl}}"},
              {"text": "📝 Ревью", "url": "{{prUrl}}/files"},
              {"text": "✅ Одобрить", "url": "{{prUrl}}/files#submit-review"}
            ]

      - name: Send review notification
        if: github.event_name == 'pull_request_review'
        uses: ./
        with:
          telegram_token: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          chat_id: ${{ secrets.TELEGRAM_CHAT_ID }}
          template: info
          message: |
            📝 **Обзор Pull Request**
            
            **PR:** #{{prNumber}} - {{prTitle}}
            **Ревьюер:** {{reviewAuthor}}
            **Состояние:** {{reviewState}}
            **ID ревью:** {{reviewId}}
            
            🏢 **Репозиторий:** {{repository}}
            👤 **Владелец:** {{repositoryOwner}}
            
            📊 **Контекст:**
            - Workflow: {{workflow}}
            - Runner: {{runnerName}} ({{runnerOs}})
            - Актор: {{actor}}
            
            💭 **Комментарий ревью:**
            {{reviewBody}}
          inline_keyboard: |
            [
              {"text": "👀 Посмотреть ревью", "url": "{{reviewUrl}}"},
              {"text": "📝 К PR", "url": "{{prUrl}}"},
              {"text": "💬 Ответить", "url": "{{prUrl}}#discussion_r"}
            ]

  discussion-notifications:
    name: Discussion Notifications
    runs-on: ubuntu-latest
    if: github.event_name == 'discussion'
    steps:
      - uses: actions/checkout@v4

      - name: Send discussion notification
        uses: ./
        with:
          telegram_token: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          chat_id: ${{ secrets.TELEGRAM_CHAT_ID }}
          template: info
          message: |
            💬 **Обсуждение {{action}}**
            
            **Заголовок:** {{discussionTitle}}
            **Автор:** {{discussionAuthor}}
            **Категория:** {{discussionCategory}}
            **ID:** {{discussionId}}
            
            🏢 **Репозиторий:** {{repository}}
            👤 **Владелец:** {{repositoryOwner}}
            🔗 **Ссылка:** {{discussionUrl}}
            
            📊 **Контекст GitHub:**
            - Workflow: {{workflow}}
            - Запуск: #{{runNumber}}
            - Runner: {{runnerName}} на {{runnerOs}}
            - Актор: {{actor}}
            - Время: {{triggeredAt}}
            
            📝 **Содержание:**
            {{discussionBody}}
          inline_keyboard: |
            [
              {"text": "💬 Присоединиться", "url": "{{discussionUrl}}"},
              {"text": "👍 Реакция", "url": "{{discussionUrl}}"},
              {"text": "✍️ Ответить", "url": "{{discussionUrl}}#discussioncomment-new"}
            ]

  health-check:
    name: Repository Health Check
    runs-on: ubuntu-latest
    if: github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.event.inputs.check_type == 'health')
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Run health checks
        id: health-check
        run: |
          echo "Running repository health checks..."

          # Check for security vulnerabilities
          AUDIT_RESULT=$(npm audit --audit-level high --json || echo '{}')
          VULNERABILITIES=$(echo "$AUDIT_RESULT" | jq -r '.metadata.vulnerabilities.total // 0')

          # Check test coverage
          npm run test:coverage > /dev/null 2>&1 || true
          if [ -f coverage/coverage-summary.json ]; then
            COVERAGE=$(jq -r '.total.lines.pct' coverage/coverage-summary.json)
          else
            COVERAGE="N/A"
          fi

          # Check for outdated dependencies
          OUTDATED=$(npm outdated --json 2>/dev/null | jq -r 'keys | length' || echo "0")

          # Check repository size
          REPO_SIZE=$(du -sh . | cut -f1)

          echo "vulnerabilities=$VULNERABILITIES" >> $GITHUB_OUTPUT
          echo "coverage=$COVERAGE" >> $GITHUB_OUTPUT
          echo "outdated=$OUTDATED" >> $GITHUB_OUTPUT
          echo "repo_size=$REPO_SIZE" >> $GITHUB_OUTPUT

          # Determine health status
          if [ "$VULNERABILITIES" -gt 0 ]; then
            echo "status=warning" >> $GITHUB_OUTPUT
          else
            echo "status=success" >> $GITHUB_OUTPUT
          fi

      - name: Send health check notification
        uses: ./
        with:
          telegram_token: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          chat_id: ${{ secrets.TELEGRAM_CHAT_ID }}
          template: ${{ steps.health-check.outputs.status }}
          message: |
            🏥 **Проверка здоровья репозитория**
            
            **Результат:** ${{ steps.health-check.outputs.status == 'success' && '✅ Отличное здоровье' || '⚠️ Требует внимания' }}
            **Размер:** ${{ steps.health-check.outputs.repo_size }}
            **Покрытие тестами:** ${{ steps.health-check.outputs.coverage }}%
            **Уязвимости:** ${{ steps.health-check.outputs.vulnerabilities }}
            **Устаревшие пакеты:** ${{ steps.health-check.outputs.outdated }}
            
            🏢 **Репозиторий:** {{repository}}
            👤 **Владелец:** {{repositoryOwner}}
            🆔 **ID репозитория:** {{repositoryId}}
            
            📊 **Контекст GitHub:**
            - Workflow: {{workflow}}
            - Задача: {{job}}
            - Запуск: #{{runNumber}} (ID: {{runId}})
            - Runner: {{runnerName}} на {{runnerOs}}
            - Архитектура: {{runnerArch}}
            - Время: {{triggeredAt}}
            
            🌐 **Окружение:**
            - Actions Environment: {{actionsEnvironment}}
            - Workspace: {{workspace}}
            - Tool Cache: {{runnerToolCache}}
            - Temp: {{runnerTemp}}
            
            🔗 **GitHub URLs:**
            - Сервер: {{serverUrl}}
            - API: {{apiUrl}}
            - GraphQL: {{graphqlUrl}}
          inline_keyboard: |
            [
              {"text": "📊 Действия", "url": "{{serverUrl}}/{{repository}}/actions"},
              {"text": "🔒 Безопасность", "url": "{{serverUrl}}/{{repository}}/security"},
              {"text": "📈 Аналитика", "url": "{{serverUrl}}/{{repository}}/pulse"},
              {"text": "🔧 Настройки", "url": "{{serverUrl}}/{{repository}}/settings"}
            ]

  repository-stats:
    name: Repository Statistics
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.check_type == 'stats'
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Fetch full history for stats

      - name: Generate repository statistics
        id: stats
        run: |
          # Get commit statistics
          TOTAL_COMMITS=$(git rev-list --all --count)
          COMMITS_THIS_MONTH=$(git rev-list --since="1 month ago" --count HEAD)
          CONTRIBUTORS=$(git log --format='%ae' | sort | uniq | wc -l)

          # Get file statistics
          TOTAL_FILES=$(find . -type f -not -path './.git/*' -not -path './node_modules/*' | wc -l)
          CODE_LINES=$(find . -name "*.js" -not -path './node_modules/*' -not -path './.git/*' | xargs wc -l | tail -1 | awk '{print $1}')

          # Get latest release
          LATEST_RELEASE=$(git describe --tags --abbrev=0 2>/dev/null || echo "No releases")

          echo "total_commits=$TOTAL_COMMITS" >> $GITHUB_OUTPUT
          echo "commits_this_month=$COMMITS_THIS_MONTH" >> $GITHUB_OUTPUT
          echo "contributors=$CONTRIBUTORS" >> $GITHUB_OUTPUT
          echo "total_files=$TOTAL_FILES" >> $GITHUB_OUTPUT
          echo "code_lines=$CODE_LINES" >> $GITHUB_OUTPUT
          echo "latest_release=$LATEST_RELEASE" >> $GITHUB_OUTPUT

      - name: Send statistics report
        uses: ./
        with:
          telegram_token: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          chat_id: ${{ secrets.TELEGRAM_CHAT_ID }}
          template: info
          message: |
            📊 **Статистика репозитория**
            
            **Всего коммитов:** ${{ steps.stats.outputs.total_commits }}
            **За месяц:** ${{ steps.stats.outputs.commits_this_month }}
            **Участников:** ${{ steps.stats.outputs.contributors }}
            **Файлов:** ${{ steps.stats.outputs.total_files }}
            **Строк кода:** ${{ steps.stats.outputs.code_lines }}
            **Последний релиз:** ${{ steps.stats.outputs.latest_release }}
            
            🏢 **Репозиторий:** {{repository}}
            👤 **Владелец:** {{repositoryOwner}}
            🆔 **ID:** {{repositoryId}}
            
            📊 **Контекст GitHub:**
            - Workflow: {{workflow}}
            - Задача: {{job}}
            - Запуск: #{{runNumber}}
            - Runner: {{runnerName}} на {{runnerOs}}
            - Архитектура: {{runnerArch}}
            - Время: {{triggeredAt}}
            
            🌐 **Окружение:**
            - Actions Environment: {{actionsEnvironment}}
            - Workspace: {{workspace}}
            - Server: {{serverUrl}}
          inline_keyboard: |
            [
              {"text": "📈 Аналитика", "url": "{{serverUrl}}/{{repository}}/pulse"},
              {"text": "👥 Участники", "url": "{{serverUrl}}/{{repository}}/graphs/contributors"},
              {"text": "📊 Трафик", "url": "{{serverUrl}}/{{repository}}/graphs/traffic"},
              {"text": "🏷️ Релизы", "url": "{{serverUrl}}/{{repository}}/releases"}
            ]
